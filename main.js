// This is the main custmized javascript  which will handle form submission
// However the form was removed  from the main project and its not necessary
// 
     /*document.getElementById('uniqueFormId').addEventListener('submit', function(event) {
           event.preventDefault(); // Prevent the form from submitting normally
            // Hide the form
            document.getElementById('uniqueFormId').style.display = 'none';
            // Show the modal
            $('#thankYouModalId').modal('show');
        });*/
